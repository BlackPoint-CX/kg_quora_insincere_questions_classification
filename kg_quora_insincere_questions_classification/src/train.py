from model_helper import build_train_tuple
import tensorflow as tf


def train(hparams):
    #
    # train_tuple = build_train_tuple()
    #
    # train_sess = tf.Session()
    #
    #
    # with train_tuple.graph.as_default():
    #     # train_model,global_step = build_or_load_model(train_tuple.model,hparams.model_dir)
    #
    # while global_step < hparams.num_train
    pass